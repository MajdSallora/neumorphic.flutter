import 'package:flutter/widgets.dart';

class NeuTorch extends InheritedWidget {
  NeuTorch() : super(child: const SizedBox());

  @override
  bool updateShouldNotify(InheritedWidget oldWidget) {
    throw UnimplementedError();
  }
}
